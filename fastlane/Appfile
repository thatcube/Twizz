app_identifier("com.thatcube.Twizz")
team_id("N8Z5T4AK3X")

# Apple ID login is not used; authentication is handled by an App Store Connect
# API key (see the Fastfile / .env.fastlane.example). These are kept here only
# for reference and for any lane that explicitly opts into user auth.
